# 凭据静态加密采用记录级 AEAD

上游密钥与订阅令牌必须落盘才能跨重启存活，明文存储一旦数据文件泄漏即等于密钥泄漏。因此每条凭据单独用 AEAD 加密后入库，主密钥与数据分离，不进数据库。

## 备选方案

- 明文落盘只靠文件权限。实现最简，但备份、同步、误传都会把密钥带出去
- 整库加密。密钥管理粒度粗，改一条凭据要重写整个文件
- 用户口令派生主密钥。安全性取决于口令强度，且每次启动都要输入

## 影响

主密钥取自操作系统凭据库，换机器需要重新导入。加密后无法在数据库里按凭据明文检索，需要额外的索引字段。
