# 内部表示采用自有统一语言

上游协议各有各的字段与语义，直接拿其中一套当作内部中转格式，会让领域模型被外部协议的演化牵着走，也表达不了它们共同承载的业务概念。入站与出站协议因此都通过适配器翻译成领域自己的表达，基准协议固定为四套，其余上游按变体接入。

## 备选方案

- 以 OpenAI Chat Completions 为内部格式。省去一层转换，但把领域模型绑在单一上游协议的字段上，上游改字段我们就得跟着改
- 成对直转，不设中间层。实现显式，但新增一套协议要与已有每一套分别配对，复杂度随协议数平方增长
