# 前端产物内嵌进单二进制

管理界面只服务本机与局域网，更新节奏与后端一致，内嵌进二进制可以把部署简化成替换一个文件。release 构建期用 rust-embed 嵌入，debug 期从文件系统读取，开发时仍有热更新。

## 备选方案

- 前后端分离部署。发布节奏与边缘缓存更灵活，但要额外维护静态资源托管与跨域配置
- 完全不做界面，只用配置文件。省掉整个前端，但凭证与模型映射的日常增删改体验很差

## 影响

二进制体积增大，前端改动需要重新编译后端。静态资源不走 CDN，压缩与缓存协商由后端承担。
