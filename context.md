# 统一语言

## 术语

### 接入与转发

**访问密钥**:
客户端接入时使用的凭据
等价: AccessKey
避免: API Key, 令牌, Token

**转发**:
一次请求从入站协议到出站协议的完整处理
等价: Relay
避免: 代理, 透传

**内部统一表示**:
领域内部表达一次模型调用的语言
等价: CanonicalRequest
避免: 中间格式, 标准格式

### 模型与调度

**模型别名**:
客户端请求时使用的模型名
等价: ModelAlias
避免: 模型映射, 模型配置

**上游引用**:
别名指向的一个具体上游与模型
等价: UpstreamRef
避免: 渠道模型, 上游模型

**选择策略**:
别名下多个上游引用的挑选方式
等价: SelectionStrategy
避免: 负载均衡, 路由算法

**调度**:
为一次请求选定凭证的决策
等价: Routing
避免: 路由, 负载均衡

**重试策略**:
失败后是否换凭证再试以及换几个的规则
等价: RetryPolicy
避免: 重试次数, 容错

**会话粘性**:
同一会话尽量固定使用同一凭证
等价: SessionAffinity
避免: 会话绑定, 固定路由

**冷却**:
凭证失败后暂时不可用的状态
等价: Cooldown
避免: 熔断, 禁用

### 凭证

**凭证**:
访问某个上游的身份凭据
等价: Credential
避免: 密钥, 账号, Token

**密钥凭证**:
长期有效的上游密钥
等价: ApiKeyCredential
避免: 普通凭证

**订阅凭证**:
由订阅账号授权、会过期并需要刷新的凭据
等价: SubscriptionCredential
避免: OAuth 账号, 订阅号

**健康状态**:
凭证当前是否可用以及不可用的原因
等价: Health
避免: 状态, 可用性

### 计量

**用量记录**:
一次请求消耗的 token 与折算金额
等价: UsageRecord
避免: 日志, 账单

**计量**:
收集用量记录并按天聚合的观察能力
等价: Metering
避免: 统计, 计费
