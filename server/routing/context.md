# 调度

决定每次请求落到哪个凭证，处理失败重试与会话粘性。

## 术语

**调度**:
为一次请求选定凭证的决策
等价: Routing
避免: 路由, 负载均衡

**重试策略**:
失败后是否换凭证再试以及换几个的规则
等价: RetryPolicy
避免: 重试次数, 容错

**会话粘性**:
同一会话尽量固定使用同一凭证
等价: SessionAffinity
避免: 会话绑定, 固定路由

**冷却**:
凭证失败后暂时不可用的状态
等价: Cooldown
避免: 熔断, 禁用
